const mongoose = require('mongoose');
const Schema = mongoose.Schema;

/**const UserPricuserPriceSchemaeSchema = new Schema({
    id: Number,
    user_id: String,
    product_id: String,
    new_price: String
});

const UserPrice = mongoose.model('UserPrice', userPriceSchema);

module.exports = { UserPrice };*/